Who is in?

interface features:
    create event
    sign up for event
    show number of participants
    show (calculated) cost per person
    
technical features:
    store userdata in cookie
    event data in database


Eventdata:
    default price 0
        --> if 0 don't show price
    specify free

Cookies
    on submit "count me in"
        if cookie doesnt exist
            create new uuid
            store in cookie
            use on submit

        if exists
            use on submit

        if exist but want to submit other
            creta new uuid
            use on submit

